name: Build
on: workflow_dispatch
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm init -y
      - run: npm install electron
      - run: echo "console.log('test')" > main.js
      - uses: actions/upload-artifact@v4
        with:
          name: output
          path: main.js
